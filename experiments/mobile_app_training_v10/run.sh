#!/bin/bash
# v10 driver. Stub — fill in after v7/v8/v9 EVAL.md files are populated.
#
# Typical shape (uncomment + fill once recipe.yaml exists):
#
# SCRIPT_DPATH="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
# RECIPE="$SCRIPT_DPATH/recipe.yaml"
# exec kwcoco-detector-kit recipe-run "$RECIPE" "$@"
set -euo pipefail
echo "[v10] recipe.yaml not yet committed."
echo "[v10] Fill in the recipe after v7/v8/v9 results are in, then"
echo "[v10] uncomment the recipe-run invocation in this file."
exit 1
