# Gradle / Android build outputs
build/
.gradle/
local.properties
*.iml
.idea/
captures/
.cxx/

# Kotlin Multiplatform
.kotlin/

# Flutter (if used)
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/

# Rust (if used)
target/

# Model assets — never commit binaries (see README.md §1)
*.onnx
*.tflite
*.pte
*.pt
*.ckpt
*.safetensors
*.aar
*.so

# Captured failure cases — local only
failure_cases/

# Generated benchmark JSON reports (re-generate via scripts/bench_desktop.sh).
# .keep below preserves the dir.
docs/benchmarks/*.json
!docs/benchmarks/.keep

# OS / editor noise
.DS_Store
*.swp
